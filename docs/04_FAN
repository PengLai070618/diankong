项目：软件第四题

一、项目概述
本项目的目标是构建一个完整的直流电机速度闭环控制系统。用户通过旋转电位器设定期望转速，STM32读取电位器电压作为目标值，同时通过编码器实时测量电机实际转速，两者送入PID控制器计算误差，输出PWM信号驱动电机，形成闭环控制。
系统涉及三个核心环节：输入采集（电位器ADC）、反馈测量（编码器测速）、控制计算（增量式PID）。三个环节协同工作，使电机转速跟随电位器设定值变化。

二、硬件架构
主控与驱动
主控芯片采用STM32F103C8T6，电机驱动使用TB6612FNG模块。TB6612需要两路信号：PWM占空比控制转速，AIN1/AIN2两个GPIO控制正反转方向。TB6612的VM引脚需要外接12V电源供电以驱动电机。
编码器测速
电机尾部的霍尔编码器输出AB两路正交脉冲信号，连接到STM32的TIM2编码器接口。定时器配置为同时检测两相的上升沿和下降沿，实现四倍频计数。速度的计算方式是每50毫秒读取一次计数值，与上次读数相减得到脉冲增量，该增量直接作为当前速度的反馈值。
电位器输入
电位器采用分压电路接法，一端接3.3V，另一端接地，中间抽头连接ADC输入引脚PA2。转动旋钮时，PA2上的电压在0至3.3伏之间线性变化，ADC采样值随之在0至4095之间变化。这个值直接映射为目标速度，数值范围是0至1000。
三、软件架构
模块分层
软件按照功能分为三个层次。底层是硬件抽象层，封装了ADC读取、编码器读取、PWM输出和GPIO方向控制这些基础操作。中间是算法层，实现了增量式PID的计算逻辑。最上层是应用层，由FreeRTOS调度两个独立任务：ADC任务负责采样电位器并更新目标值，PID任务负责执行控制算法并更新PWM输出。
任务调度设计
两个任务的设计基于对控制周期的考量。ADC任务运行周期为100毫秒，因为电位器由人手转动，变化速度较慢，不需要过于频繁采样。PID任务运行周期为50毫秒，需要更快的响应速度来及时修正偏差，因此优先级设置为高于ADC任务。FreeRTOS的抢占式调度机制保证PID任务在需要时能够立即执行。
数据流向
整个控制循环的数据流向是单向闭环：ADC采样值通过归一化计算得到目标速度，送入PID控制器；编码器的两次读数之差作为实际速度，同样送入PID控制器；控制器计算输出值，根据正负方向调用正转或反转函数，将限幅后的PWM值写入定时器比较寄存器；电机响应PWM变化后，编码器读数随之更新，下一轮控制循环读取新的反馈值，形成持续的闭环调节。

