CAN通信模块
作者： 赖鹏
日期： 2026年8月12日
硬件平台： STM32F103C8T6 × 2，TJA1050 CAN收发器 × 2

一、设计思路
实现两块STM32开发板之间的CAN通信。
主机（MASTER）：检测按键（PA0）按下，通过CAN总线向从机发送数据 0x11。
从机（SLAVE）：收到 0x11 后，回复数据 0x22，并翻转板载LED（PC13）。
主机：收到从机回复的 0x22 后，通过串口打印 Received ACK: 0x22。
通信协议符合CAN 2.0B标准，使用标准帧（11位ID），波特率1Mbps。

二，架构
1，模块划分main.c包含主，从机发送接收信号逻辑，以及从机板载LED翻转指示。
can.c包含启动/过滤器/中断使能，接收回调函数
2，使用预编译宏切换，一套代码烧录不同功能
3，CAN通信函数

三，遇到的问题（）
最折磨的一集，问题太多了（芯片无法启动，LED点灯不闪，无法显示串口数据，按键一次多次发送，
串口消息持续输出，从机收到数据后卡死）