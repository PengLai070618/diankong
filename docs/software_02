题目名称：上位机调试（VOFA+ 舵机控制与可视化）
完成日期：2026年8月9日
作者：赖鹏
1. 设计思路
本任务旨在实现STM32 与 VOFA+ 上位机之间的双向通信，形成“指令下发 → 动作执行 → 状态回传 → 可视化显示”的闭环控制系统。

核心设计：
下行链路（上位机 → 单片机）：VOFA+ 通过串口发送角度字符串（如 45\n），单片机在串口中断中解析指令，调用 Servo_SetAngle() 驱动舵机。
上行链路（单片机 → 上位机）：舵机动作完成后，通过 Vofa_JustFloat_Send() 将当前角度和脉宽打包成字节流，经串口实时回传，在 VOFA+ 波形图上动态显示。

工程架构：
pwm.c/h：封装舵机角度控制函数，隔离底层硬件
uart.c/h：实现 printf 重定向，提供串口字节级发送接口
vofa.c/h：实现 JustFloat 协议，将浮点数组打包为字节流并发送
main.c：初始化外设，实现串口中断回调与主循环逻辑

2. 遇到的问题与解决方案(部分)
问题 1：烧录失败，ST-Link 提示 No device found on target
原因：新板子休眠或锁
解决：使用板子上的RESET键和CubeProgrammer连接
问题 2：串口中断不响应，VOFA+ 发送数据无反应
原因：CubeMX 中未使能 USART1 全局中断（NVIC 设置遗漏）。
解决：在 CubeMX 的 NVIC Settings 中勾选 USART1 global interrupt，重新生成代码。

文档版本：V1.0
最后更新：2026年8月9日